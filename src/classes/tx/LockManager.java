package classes.tx;

import classes.storageengine.Lock;
import classes.storageengine.Resource;
import enums.LockMode;

public class LockManager {
    public Lock acquire(LockMode mode, Resource resource, Transaction tx) {
        return null;
    }
}